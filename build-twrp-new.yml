name: Build TWRP for Apollo (Android 16 FBE Decrypt)

on:
  workflow_dispatch:
    inputs:
      device:
        description: 'Device codename'
        required: true
        default: 'apollo'
      manifest_branch:
        description: 'TWRP manifest branch'
        required: true
        default: 'twrp-16.0'

env:
  MANIFEST_URL: https://github.com/TWRP-Test/platform_manifest_twrp_aosp.git
  MANIFEST_BRANCH: twrp-16.0
  DEVICE_TREE_URL: https://github.com/PitchBlackRecoveryDevices/android_device_xiaomi_apollo
  DEVICE_TREE_BRANCH: android-10.0
  DEVICE_CODE: apollo
  USE_CCACHE: 1
  CCACHE_DIR: /home/runner/.ccache
  CCACHE_EXEC: /usr/bin/ccache

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Display configuration
        run: |
          echo "=============================================="
          echo "  TWRP Build for Apollo (Mi 10T / K30S Ultra)"
          echo "  Android 16 FBE Decryption Support"
          echo "=============================================="
          echo "Device: ${{ github.event.inputs.device || 'apollo' }}"
          echo "Manifest: $MANIFEST_URL ($MANIFEST_BRANCH)"
          echo "Lunch: twrp_apollo-bp2a-eng"
          echo "Runner: ubuntu-22.04 (2-core, 7GB RAM + 8GB swap)"
          echo "=============================================="

      - name: Create swap space
        continue-on-error: true
        run: |
          echo "Creating 8GB swap file..."
          sudo rm -f /swapfile
          sudo fallocate -l 8G /swapfile 2>/dev/null || sudo dd if=/dev/zero of=/swapfile bs=1M count=8192 status=none
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile 2>/dev/null || echo "swap creation failed, continuing without swap"
          free -h

      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h /
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/.ghcup
          sudo apt-get clean
          echo "Disk space after cleanup:"
          df -h /

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git-core gnupg flex bison build-essential zip curl \
            zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 \
            libncurses-dev lib32ncurses-dev x11proto-core-dev libx11-dev \
            lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig \
            python3 python3-pip repo bc ccache rsync libssl-dev \
            liblz4-tool lz4 zstd

      - name: Set up ccache
        uses: actions/cache@v4
        with:
          path: /home/runner/.ccache
          key: ccache-apollo-twrp-${{ github.sha }}
          restore-keys: |
            ccache-apollo-twrp-

      - name: Configure git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git config --global color.ui false

      - name: Initialize TWRP source
        run: |
          mkdir -p ~/android/twrp
          cd ~/android/twrp
          repo init --depth=1 -u $MANIFEST_URL -b $MANIFEST_BRANCH
          repo sync -c --no-tags --no-clone-bundle -j4

      - name: Clone device tree (for prebuilt kernel and vendor binaries)
        run: |
          cd ~/android/twrp
          git clone --depth=1 -b $DEVICE_TREE_BRANCH $DEVICE_TREE_URL device/xiaomi/apollo

      - name: Clone TWRP Qualcomm common (for FBE decryption)
        run: |
          cd ~/android/twrp
          git clone --depth=1 -b android-14.1 https://github.com/TeamWin/android_device_qcom_twrp-common.git device/qcom/twrp-common
          # Create dummy twrp-common.mk (referenced by build system but not in repo)
          touch device/qcom/twrp-common/twrp-common.mk

      - name: Rewrite device tree for Android 16
        run: |
          cd ~/android/twrp/device/xiaomi/apollo

          # Remove old Android 10 mk files
          rm -f AndroidProducts.mk omni_apollo.mk BoardConfig.mk Android.mk

          # Remove old addons (outdated Magisk zips, SafetyNet, etc.)
          rm -rf recovery/root/addon

          # ============================================================
          # Create AndroidProducts.mk (Android 16 lunch format)
          # ============================================================
          cat > AndroidProducts.mk << 'PRODUCTEOF'
          PRODUCT_MAKEFILES := \
              $(LOCAL_DIR)/twrp_apollo.mk

          COMMON_LUNCH_CHOICES := \
              twrp_apollo-bp2a-eng
          PRODUCTEOF

          # ============================================================
          # Create twrp_apollo.mk (TWRP 16.0 product configuration)
          # ============================================================
          cat > twrp_apollo.mk << 'TWRPEOF'
          # TWRP for Xiaomi Mi 10T / Redmi K30S Ultra (apollo)
          # Android 16 / TWRP 16.0 - Rewritten from scratch

          DEVICE_PATH := device/xiaomi/apollo

          # Inherit TWRP common configuration (replaces vendor/omni/config/common.mk)
          $(call inherit-product, vendor/twrp/config/common.mk)

          # Inherit AOSP core
          $(call inherit-product, $(SRC_TARGET_DIR)/product/core_minimal.mk)

          # Device identifier
          PRODUCT_DEVICE := apollo
          PRODUCT_NAME := twrp_apollo
          PRODUCT_BRAND := Xiaomi
          PRODUCT_MODEL := M2007J3SG
          PRODUCT_MANUFACTURER := Xiaomi
          PRODUCT_RELEASE_NAME := apollo

          # Dynamic partitions
          PRODUCT_USE_DYNAMIC_PARTITIONS := true

          # FBE Decryption support (built from device/qcom/twrp-common)
          PRODUCT_PACKAGES += \
              qcom_decrypt_fbe \
              qcom_decrypt
          TWRPEOF

          # ============================================================
          # Create BoardConfig.mk (Android 16 compatible)
          # ============================================================
          cat > BoardConfig.mk << 'BOARDEOF'
          # BoardConfig for Xiaomi Mi 10T / Redmi K30S Ultra (apollo)
          # Platform: SM8250 / kona
          # Android 16 / TWRP 16.0 - Rewritten from scratch

          DEVICE_PATH := device/xiaomi/apollo

          # Architecture
          TARGET_ARCH := arm64
          TARGET_ARCH_VARIANT := armv8-a
          TARGET_CPU_ABI := arm64-v8a
          TARGET_CPU_ABI2 :=
          TARGET_CPU_VARIANT := kryo

          TARGET_2ND_ARCH := arm
          TARGET_2ND_ARCH_VARIANT := armv8-a
          TARGET_2ND_CPU_ABI := armeabi-v7a
          TARGET_2ND_CPU_ABI2 := armeabi
          TARGET_2ND_CPU_VARIANT := cortex-a73

          ENABLE_CPUSETS := true
          ENABLE_SCHEDBOOST := true

          # Bootloader
          TARGET_BOOTLOADER_BOARD_NAME := kona
          TARGET_NO_BOOTLOADER := true
          TARGET_USES_UEFI := true

          # Platform
          TARGET_BOARD_PLATFORM := kona
          TARGET_BOARD_PLATFORM_GPU := qcom-adreno650
          QCOM_BOARD_PLATFORMS += kona

          # Kernel (prebuilt)
          BOARD_KERNEL_CMDLINE := console=ttyMSM0,115200n8 androidboot.hardware=qcom androidboot.console=ttyMSM0 androidboot.memcg=1 lpm_levels.sleep_disabled=1 video=vfb:640x400,bpp=32,memsize=3072000 msm_rtb.filter=0x237 service_locator.enable=1 androidboot.usbcontroller=a600000.dwc3 swiotlb=2048 loop.max_part=7 cgroup.memory=nokmem,nosocket
          BOARD_KERNEL_CMDLINE += androidboot.selinux=permissive androidboot.init_fatal_reboot_target=recovery
          BOARD_KERNEL_IMAGE_NAME := Image
          BOARD_KERNEL_PAGESIZE := 4096
          BOARD_BOOT_HEADER_VERSION := 2
          BOARD_KERNEL_BASE := 0x00000000
          BOARD_KERNEL_TAGS_OFFSET := 0x00000100
          BOARD_KERNEL_OFFSET := 0x00008000
          BOARD_KERNEL_SECOND_OFFSET := 0x00f00000
          BOARD_RAMDISK_OFFSET := 0x01000000
          BOARD_DTB_OFFSET := 0x01f00000

          TARGET_KERNEL_ARCH := arm64
          TARGET_PREBUILT_DTB := $(DEVICE_PATH)/prebuilt/dtb
          TARGET_PREBUILT_KERNEL := $(DEVICE_PATH)/prebuilt/kernel
          BOARD_PREBUILT_DTBOIMAGE := $(DEVICE_PATH)/prebuilt/recovery_dtbo
          BOARD_INCLUDE_RECOVERY_DTBO := true
          BOARD_INCLUDE_DTB_IN_BOOTIMG := true
          BOARD_MKBOOTIMG_ARGS += --base $(BOARD_KERNEL_BASE)
          BOARD_MKBOOTIMG_ARGS += --pagesize $(BOARD_KERNEL_PAGESIZE)
          BOARD_MKBOOTIMG_ARGS += --ramdisk_offset $(BOARD_RAMDISK_OFFSET)
          BOARD_MKBOOTIMG_ARGS += --tags_offset $(BOARD_KERNEL_TAGS_OFFSET)
          BOARD_MKBOOTIMG_ARGS += --kernel_offset $(BOARD_KERNEL_OFFSET)
          BOARD_MKBOOTIMG_ARGS += --second_offset $(BOARD_KERNEL_SECOND_OFFSET)
          BOARD_MKBOOTIMG_ARGS += --dtb_offset $(BOARD_DTB_OFFSET)
          BOARD_MKBOOTIMG_ARGS += --header_version $(BOARD_BOOT_HEADER_VERSION)
          BOARD_MKBOOTIMG_ARGS += --dtb $(TARGET_PREBUILT_DTB)

          # Assert
          TARGET_OTA_ASSERT_DEVICE := apollo, apollopro

          # AVB
          BOARD_AVB_ENABLE := true
          BOARD_AVB_ROLLBACK_INDEX := $(PLATFORM_SECURITY_PATCH_TIMESTAMP)

          # Partitions
          BOARD_FLASH_BLOCK_SIZE := 262144
          BOARD_BOOTIMAGE_PARTITION_SIZE := 134217728
          BOARD_RECOVERYIMAGE_PARTITION_SIZE := 134217728

          # Dynamic Partitions
          BOARD_SUPER_PARTITION_SIZE := 9126805504
          BOARD_SUPER_PARTITION_GROUPS := qti_dynamic_partitions
          BOARD_QTI_DYNAMIC_PARTITIONS_SIZE := 9126805504
          BOARD_QTI_DYNAMIC_PARTITIONS_PARTITION_LIST := product vendor system system_ext odm

          # File systems
          TARGET_USERIMAGES_USE_EXT4 := true
          TARGET_USERIMAGES_USE_F2FS := true

          # Recovery
          BOARD_HAS_LARGE_FILESYSTEM := true
          TARGET_RECOVERY_PIXEL_FORMAT := "BGRA_8888"
          BOARD_VENDORIMAGE_FILE_SYSTEM_TYPE := ext4
          TARGET_COPY_OUT_VENDOR := vendor

          # Crypto / FBE Decryption
          PLATFORM_SECURITY_PATCH := 2099-12-31
          VENDOR_SECURITY_PATCH := 2099-12-31
          TW_INCLUDE_CRYPTO := true
          TW_INCLUDE_CRYPTO_FBE := true
          TW_INCLUDE_FBE_METADATA_DECRYPT := true
          BOARD_USES_METADATA_PARTITION := true
          BOARD_USES_QCOM_FBE_DECRYPTION := true
          USE_FSCRYPT := true
          TW_USE_FSCRYPT_POLICY := 2

          # TWRP UI
          TW_THEME := portrait_hdpi
          RECOVERY_SDCARD_ON_DATA := true
          TARGET_RECOVERY_QCOM_RTC_FIX := true
          TW_EXCLUDE_DEFAULT_USB_INIT := true
          TW_EXTRA_LANGUAGES := true
          TW_INCLUDE_NTFS_3G := true
          TW_USE_TOOLBOX := true
          TW_INPUT_BLACKLIST := "hbtp_vm"
          TW_BRIGHTNESS_PATH := "/sys/class/backlight/panel0-backlight/brightness"
          TW_MAX_BRIGHTNESS := 2047
          TW_DEFAULT_BRIGHTNESS := 1200
          TWRP_INCLUDE_LOGCAT := true
          TARGET_USES_LOGD := true
          TARGET_USES_MKE2FS := true
          TW_EXCLUDE_TWRPAPP := true
          TW_NO_SCREEN_BLANK := true
          TW_DEVICE_VERSION := Android16-FBEv2
          TW_HAS_EDL_MODE := true
          TW_SUPPORT_INPUT_1_2_HAPTICS := true

          # SELinux
          TW_FORCE_SELINUX_PERMISSIVE := true
          BOARDEOF

          # ============================================================
          # Create Android.mk
          # ============================================================
          cat > Android.mk << 'MAKEEOF'
          LOCAL_PATH := $(call my-dir)

          ifeq ($(TARGET_DEVICE),apollo)
              include $(call all-makefiles-under,$(LOCAL_PATH))
              include $(CLEAR_VARS)
          endif
          MAKEEOF

          # ============================================================
          # Debug: print all created files
          # ============================================================
          echo "=============================================="
          echo "  Created files listing"
          echo "=============================================="
          ls -la *.mk
          echo ""
          echo "=== AndroidProducts.mk ==="
          cat -n AndroidProducts.mk
          echo ""
          echo "=== twrp_apollo.mk ==="
          cat -n twrp_apollo.mk
          echo ""
          echo "=== BoardConfig.mk (first 50 lines) ==="
          head -50 BoardConfig.mk
          echo ""
          echo "=== Android.mk ==="
          cat -n Android.mk
          echo ""
          echo "=== twrp-common directory ==="
          ls -la ~/android/twrp/device/qcom/twrp-common/
          echo ""
          echo "=== Prebuilt files ==="
          ls -la prebuilt/
          echo ""
          echo "=== Recovery root structure ==="
          find recovery/root -type f | head -30
          echo "=============================================="

      - name: Build recovery image
        run: |
          cd ~/android/twrp
          export ALLOW_MISSING_DEPENDENCIES=true
          export BUILD_BROKEN_DUP_RULES=true
          export BUILD_BROKEN_USES_BUILD_COPY_FILES=true
          source build/envsetup.sh
          lunch twrp_apollo-bp2a-eng
          echo "=============================================="
          echo "TARGET_PRODUCT=$TARGET_PRODUCT"
          echo "TARGET_BUILD_VARIANT=$TARGET_BUILD_VARIANT"
          echo "TARGET_RELEASE=$TARGET_RELEASE"
          echo "=============================================="
          mka recoveryimage -j2 2>&1 | tee /tmp/build_full.log | tail -500
          echo "=== Build completed ==="

      - name: Check build output
        run: |
          cd ~/android/twrp
          if [ -f out/target/product/apollo/recovery.img ]; then
            echo "=== BUILD SUCCESSFUL ==="
            ls -la out/target/product/apollo/recovery.img
            md5sum out/target/product/apollo/recovery.img
          else
            echo "=== BUILD FAILED ==="
            echo "Looking for recovery.img..."
            find out/ -name "recovery.img" 2>/dev/null || echo "No recovery.img found"
            echo ""
            echo "Last 200 lines of build log:"
            tail -200 /tmp/build_full.log 2>/dev/null || echo "No build log found"
            exit 1
          fi

      - name: Upload recovery image
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: twrp-apollo-android16-recovery
          path: ~/android/twrp/out/target/product/apollo/recovery.img
          retention-days: 30

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-apollo
          path: /tmp/build_full.log
          retention-days: 7
          if-no-files-found: ignore
